machine:
  php:
    version: 5.5.21
  environment:
    COVERALLS_REPO_TOKEN: 9FupbCG39dJvgUFNFxFE3EQOrgZUNuGvQ
test:
  override:
    - phpunit -c phpunit.xml
dependencies:
  override:
    - mkdir -p build/logs
    - composer install --dev --no-interaction
    - sed -i 's/^;//' ~/.phpenv/versions/$(phpenv global)/etc/conf.d/xdebug.ini
